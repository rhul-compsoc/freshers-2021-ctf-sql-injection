<!DOCTYPE html>
<html lang="en" class="h-100">
<head>
  {{> siteHead }}
</head>
<body class="d-flex flex-column h-100">
  {{> siteNavbar }}
  <div class="flex-grow-1">
    {{{ body }}}
  </div>
  {{> siteFooter }}
</body>
</html>
